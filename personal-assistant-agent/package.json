{
  "name": "personal-assistant-agent",
  "version": "1.0.0",
  "description": "A personal assistant agent that checks unread emails, fetches calendar schedules, and retrieves the current time.",
  "main": "src/app.ts",
  "scripts": {
    "start": "ts-node src/app.ts",
    "build": "tsc",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "googleapis": "^39.2.0",
    "ts-node": "^10.0.0",
    "typescript": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^14.0.0"
  },
  "author": "Your Name",
  "license": "MIT"
}
